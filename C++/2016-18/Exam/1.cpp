#include <iostream>
#include <cmath>
using namespace std;

int main()
{
  int xMin = 65535, yMin = 65535, x, y, cnt;
  cin >> cnt;
  for (size_t i = 0; i < cnt; i++) {
    cin >> x >> y;
    if (abs(x) == abs(y)) {
      
    }
  }
}
