QUEUE

First In, First Out(FIFO)

functions: enqueue, dequeue
markers: head[q], tail[q]

1.enqueue(q, x):
  q[tail[q]] <- x;
  if(tail[q] == length[q])
    then tail[q] <- 1;
  else tail[q] <- tail[q]+1;

2.dequeue(q,x):
  x <- q[head[q]]
  if head[q] <- 1
  else head[q] <- head[q]+1
